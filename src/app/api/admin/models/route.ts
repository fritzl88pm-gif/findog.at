import { retiredApiResponse } from "@/lib/retired-api";

export const runtime = "nodejs";
export const GET = retiredApiResponse;
export const POST = retiredApiResponse;
export const PATCH = retiredApiResponse;
